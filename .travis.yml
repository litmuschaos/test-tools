dist: trusty
sudo: required
install: true
env:
  global:
    - GOARCH=amd64
before_install:
  - sleep 15
  - sudo apt-get install -y
  - sudo apt-get install -y curl
jobs:
  include:
    - stage: build docker images
      script:
      - make build
notifications:
  email:
    recipients:
      - karthik.s@openebs.io
