#!/bin/bash
set -e

IMAGEID=$( docker images -q openebs/busybox-liveness )

if [ ! -z "${DNAME}" ] && [ ! -z "${DPASS}" ]; 
then 
  docker login -u "${DNAME}" -p "${DPASS}"; 
  #Push to docker hub repository with latest tag
  docker tag ${IMAGEID} openebs/busybox-liveness:latest
  docker push openebs/busybox-liveness:latest; 
else
  echo "No docker credentials provided. Skip uploading openebs/sysbench-client:latest to docker hub"; 
fi;
