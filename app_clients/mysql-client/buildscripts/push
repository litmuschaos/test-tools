#!/bin/bash
set -e

IMAGEID=$( docker images -q litmuschaos/mysql-client )

if [ ! -z "${DNAME}" ] && [ ! -z "${DPASS}" ]; 
then 
  docker login -u "${DNAME}" -p "${DPASS}"; 
  #Push to docker hub repository with latest tag
  docker tag ${IMAGEID} litmuschaos/mysql-client:latest
  docker push litmuschaos/mysql-client:latest; 
else
  echo "No docker credentials provided. Skip uploading litmuschaos/mysql-client:latest to docker hub"; 
fi;
